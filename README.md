# Smartphone Data Analysis & Price Prediction

Exploratory and predictive analysis of a dataset of 15,000 smartphones, using **pandas** and **numpy** for data wrangling, **scikit-learn** for price prediction, and **Plotly** / **Matplotlib + Seaborn** for visualization.

## Dataset

`smartphones.csv` — 15,000 rows × 25 columns, including `price`, `brand_name`, `ram`, `storage`, `battery_capacity`, `camera_mp`, `screen_size`, `refresh_rate`, `release_year`, `network_support`, `operating_system`, `fingerprint_sensor`, `dual_sim`, among others.

## Insights Dashboard

**[`smartphones_insights.html`](smartphones_insights.html)** is a single, self-contained, interactive page combining every KPI and chart from both scripts below into one dashboard — open it directly in a browser (no server needed; `plotly.js` is vendored locally in `plotly.min.js`, so it also works fully offline).

## Scripts

- **`analise_smartphones.py`** — exploratory data analysis (EDA) with pandas/seaborn/matplotlib. Prints descriptive statistics and text insights, saves three static PNG charts, and exports 7 interactive Plotly chart fragments plus `outputs/kpis_eda.json`.
- **`analise_preditiva.py`** — predictive analysis with scikit-learn (Linear Regression, Random Forest, Gradient Boosting). Trains models to predict smartphone price and projects market trends up to 2032, exporting five interactive Plotly HTML charts (standalone files, plus fragments) and `outputs/kpis_predictive.json`.
- **`gerar_dashboard.py`** — assembles the KPI json files and chart fragments produced by the two scripts above into `smartphones_insights.html`.

Run all three scripts from the repository root, in order:

```bash
python analise_smartphones.py
python analise_preditiva.py
python gerar_dashboard.py
```

## Chart Explanations

### Combined dashboard

`smartphones_insights.html` opens with 12 KPI cards (dataset size, brand counts, average price, 5G/fingerprint/dual-SIM adoption, best model and its error, and 2032 price/RAM projections), followed by all 12 charts listed below rendered in one scrollable page.

### Interactive EDA charts (Plotly fragments, generated by `analise_smartphones.py`)

| Fragment | What it shows |
|---|---|
| `01_top_brands.html` | Bar chart of the top 10 brands by model count. |
| `02_price_by_brand.html` | Box plot of price distribution for the top 6 brands (median, quartiles, outliers). |
| `03_os_share.html` | Donut chart of market share by operating system. |
| `04_price_by_year.html` | Line chart of average price by release year. |
| `05_ram_distribution.html` | Bar chart of device counts by RAM tier. |
| `06_storage_distribution.html` | Bar chart of device counts by storage tier. |
| `07_correlation_heatmap.html` | Interactive correlation heatmap across numeric specs. |

### Interactive predictive charts (Plotly, generated by `analise_preditiva.py`)

| File | What it shows |
|---|---|
| `01_projecao_preco.html` | Historical average smartphone price by release year, fitted with a **Linear Regression** trendline projected forward to 2032, with a shaded confidence band (±1.5 std of residuals). |
| `02_importancia_features.html` | Horizontal bar chart of **feature importances** from the Random Forest price model — shows which specs (RAM, release year, storage, encoded categoricals, etc.) drive price the most. |
| `03_real_vs_previsto.html` | Scatter plot comparing **actual vs. predicted price** for a 500-sample test subset, for both Random Forest and Gradient Boosting models, against a "perfect prediction" reference line (R² shown per model). |
| `04_tendencias_specs.html` | Multi-panel projection (up to 2032) of average RAM, battery capacity, camera resolution, refresh rate, storage, and 5G adoption rate over time, each fitted with its own linear trend. |
| `05_bubble_marcas.html` | Bubble chart of the top 15 brands: **average price** (x-axis) vs. **average camera resolution** (y-axis), bubble size = number of models, colored by brand. |

### Static charts (Matplotlib/Seaborn, generated by `analise_smartphones.py`)

| File | What it shows |
|---|---|
| `graficos_visao_geral.png` | 2×2 panel: top 10 brands by model count, price distribution boxplot for the top 6 brands, operating system market share (pie chart), and average price by release year. |
| `correlacao.png` | Correlation heatmap across numeric specs (price, battery, RAM, storage, camera, screen size, refresh rate, fast charging, weight, thickness). |
| `ram_storage.png` | Side-by-side bar charts of the RAM (GB) and internal storage (GB) distributions across all devices. |

## Key Findings

- Random Forest slightly outperforms Gradient Boosting for price prediction (lower MAE, higher R²).
- RAM and release year are the strongest price predictors — camera resolution and refresh rate matter less.
- Prices are projected to grow moderately through 2032, driven by faster chips and higher-refresh-rate displays.
- RAM and storage are expected to roughly double in mid-range segments by 2032.
- 5G adoption is projected to exceed 80% of global releases before 2030.

## Tech Stack

`pandas` · `numpy` · `scikit-learn` · `plotly` · `matplotlib` · `seaborn`

## Repository Layout

```
smartphones.csv                 # source dataset
analise_smartphones.py          # EDA script (pandas/seaborn/matplotlib + Plotly fragments)
analise_preditiva.py            # predictive script (scikit-learn + Plotly)
gerar_dashboard.py              # assembles fragments + KPIs into the dashboard
smartphones_insights.html       # combined interactive insights dashboard
plotly.min.js                   # vendored Plotly.js (used by the dashboard)
01..05_*.html                   # standalone interactive charts (predictive)
outputs/fragments/*.html        # chart fragments used by the dashboard
outputs/kpis_*.json             # KPI values used by the dashboard
*.png                           # static EDA charts
```
